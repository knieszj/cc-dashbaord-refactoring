import { render, screen } from "@testing-library/react";
import CategoryCard from "./Category-Card/CategoryCard";
import { shallow, mount } from "enzyme";
import React from "react";
import { Button, Card } from "react-bootstrap";

describe("When the Card Component is displayed", () => {
  it("Should display a title", () => {
    // const cardWrapper = shallow(<CategoryCard category={"Fitness"} />);

    // expect(cardWrapper.containsMatchingElement('.card-title h5')).toHaveProperty()
//     expect(cardWrapper.find("Styled(Card.Title)")).toHaveLength(1);
//   });
// });

// describe("placeholder", () => {
//     it('should hold a place', () => {
//         expect(true).toEqual(true);
//     })
  })
})